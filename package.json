{
  "name": "formsquare",
  "version": "0.1.2",
  "description": "Serialize html5 forms the smart way",
  "keywords": [
    "form",
    "serialize",
    "html",
    "json"
  ],
  "main": "lib/formsquare.js",
  "scripts": {
    "build": "mkdir -p lib && echo \"export const NAME = \\\"${npm_package_name}\\\";\nexport const VERSION = \\\"${npm_package_version}\\\";\n\" | cat - src/formsquare.js | babel -f src/formsquare.js > lib/formsquare.js",
    "prebuild": "npm run lint",
    "clean": "rm dist/* lib/*",
    "dist": "mkdir -p dist && browserify -s formsquare lib/formsquare.js > dist/formsquare.js",
    "predist": "npm run build",
    "lint": "eslint src/*.js tests/*.js",
    "prepublish": "npm run build",
    "test": "browserify -t babelify tests/*.js | tap-closer | smokestack -b firefox | tap-spec",
    "test:build": "npm test",
    "pretest:build": "npm run build"
  },
  "author": "Rúnar Berg Baugsson Sigríðarson <runarberg@zoho.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-core": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "eslint": "^2.3.0",
    "smokestack": "^3.4.1",
    "tap-closer": "^1.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.0"
  },
  "dependencies": {
    "ramda": "^0.19.1"
  }
}
