{
  "name": "formsquare",
  "version": "0.2.0",
  "description": "Serialize html5 forms the smart way",
  "keywords": [
    "form",
    "serialize",
    "html",
    "json"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/runarberg/formsquare.git"
  },
  "scripts": {
    "build": "mkdir -p lib && echo \"module.exports = require(\\\"./lib/formsquare\\\");\n\nmodule.exports.NAME = \\\"${npm_package_name}\\\";\nmodule.exports.VERSION = \\\"${npm_package_version}\\\";\" > index.js && babel src/ -d lib",
    "prebuild": "npm run lint",
    "clean": "rm -fr dist lib index.js",
    "dist": "mkdir -p dist && browserify -s formsquare index.js > dist/formsquare.js",
    "predist": "npm run build",
    "lint": "eslint src/*.js tests/*.js",
    "prepublish": "npm run build",
    "test": "browserify -t babelify tests/*.js | testling | tap-spec",
    "test:build": "npm test",
    "pretest:build": "npm run build"
  },
  "author": "Rúnar Berg Baugsson Sigríðarson <runarberg@zoho.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-core": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "eslint": "^2.3.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.0",
    "testling": "^1.7.1"
  },
  "dependencies": {
    "ramda": "^0.19.1"
  }
}
